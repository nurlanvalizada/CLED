@model CledAcademy.Core.Domain.Test
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Create Test";
}

<section class="content">
    <div class="container-fluid">
        <div class="block-header">
            <h2>Add Test</h2>
        </div>

        <!-- Vertical Layout -->
        <div class="row clearfix">
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                <div class="card">
                    <div class="body">
                        <form asp-controller="Admin" asp-action="TestCreate" method="post">
                            <input type="hidden" asp-for="CourseId" value="@Context.Request.Query["courseId"]"/>
                            <label asp-for="Content"></label>
                            <div class="form-group">
                                <div class="form-line">
                                    <textarea asp-for="Content" class="form-control" placeholder="Enter content"></textarea>
                                </div>
                            </div>
                            <label asp-for="TestType"></label>
                            <div class="form-group">
                                <div class="form-line">
                                   <input asp-for="TestType" class="form-control" placeholder="Enter test type"/>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-primary m-t-15 waves-effect">SAVE</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        <!-- #END# Vertical Layout -->
    </div>
</section>
